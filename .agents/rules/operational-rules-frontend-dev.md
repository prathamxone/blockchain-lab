---
trigger: always_on
---

**Follow the below OPERATIONAL RULES**
```markdown
OPERATIONAL RULES:
1) ALWAYS use a todo list for performing the tasks and to maintain sequential flow, consistency and context without hallucination.
2) ALWAYS use MCP Server tools for referring the correct docs, code strategies and researching web based content.
3) ALWAYS use Built-in tools for performing workspace operations like search file, read file, edit file, execute command, use VS Code features, create todo list, etc.
4) ALWAYS Prompt the user (me) in case of missing context, need for clarity, manual help and human in the loop scenarios.
5) ALWAYS perform a complete mandatory re-read to these major files `Exp-6/EXP-6_IDEA.md` (lines 197), `Exp-6/docs/FEATURE_FRONTEND.md` (lines 1307) and `Exp-6/EXP-6_FRONTEND_PLAN.md` (lines 1809) in case of context missing, hallucination or clarity drift during the execution of DVote (MVP + Frontend).
6) ALWAYS re-read/trace the execution walkthrough and walkthrough checkpoint in memory for DVote (MVP `Exp-6/EXP-6_FRONTEND_EXECUTION_WALKTHROUGH.md` to understand and maintain the structured flow to follow after intermediate pauses, session resumes, user MET's, etc. without assumption in development flow.
7) ALWAYS pause, reflect and read the relevant docs, snippets, design flows, etc. from MCP or Web before writing the Core Technical Files of DVote (MVP + Frontend).

---

8) RUN Bash (Linux) Terminal commands/Semantic Search to get the (no. of lines in a file, specific sections of the file, size of the file, tree structure of the project and Git status/commit history of the project).
9) ALWAYS read a single file at a time. DO NOT read multiple files at once or perform parallel processing while reading the files.
10) ALWAYS create/edit/modify a single file at a time. DO NOT create/edit/modify multiple files at once or perform parallel processing while writing the files.
11) ALWAYS create or write the content in a file with the chunks of 300-line intervals for maintaining model context limit and preventing hallucinations.
12) DO NOT create or write the content in a file all at once.
13) USE `askQuestions` tool during execution and planning for gathering quick answers and suggestions from user (me) for discussion, confirmation and clarity.
14) USE optionally the Stitch MCP server tools for creating UI Design and minimal Frontend Code. Use these tools very carefully as the Stitch is currently in Beta as per Google but is able to create some industry standard designs if used strategically.
```